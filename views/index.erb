<!DOCTYPE html>
<html>
  <head>
    <meta charset='UTF-8' />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Itsmyparty</title>
    <link href='//netdna.bootstrapcdn.com/twitter-bootstrap/2.3.1/css/bootstrap-combined.min.css' rel='stylesheet' />
  </head>
  <body class='container'>
    <p> Hi <%= @personname %>
    <p>Jij of jullie zijn uitgenodigd  voor mijn verjaardag op 15 december!</p>
    <form action='register' method='post'>
    <input type=hidden name=person value= "<%= guid %>" />
       Aanwezig? <% Choices.each do |id, text| %>
          <li>
            <label class='radio'>
              <input type='radio' name='presence' value='<%= id %>' id='vote_<%= id %>' />
              <%= text %>
            </label>     
          </li>
        <% end %>
       Met partner?
        <% Partner.each do |id, text| %>
          <li>
            <label class='radio'>
              <input type='radio' name='partner' value='<%= id %>' id='partner_<%= id %>' />
              <%= text %>
            </label>     
          </li>
        <% end %>
        
      </ul>
     
      <button type='submit' class='btn btn-primary'>Save</button>
    </form>
  </body>
</html>
